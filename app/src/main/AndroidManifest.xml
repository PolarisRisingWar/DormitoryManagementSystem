<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.dormitorymanagementsystem"
    >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESSCOARSELOCATION" />
    <uses-permission android:name="android.permission.ACCESSFINELOCATION" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LostFoundActivity"
            android:windowSoftInputMode="stateHidden|stateAlwaysHidden"></activity>
        <activity android:name=".NewDangerActivity" />
        <activity android:name=".OldDangerActivity" />
        <activity android:name=".BigActivity" />
        <activity android:name=".OuterActivity" />
        <activity android:name=".LeftActivity" />
        <activity android:name=".DailyOutInActivity" />
        <activity android:name=".SuggestionActivity" />
        <activity android:name=".NoteActivity" />
        <activity
            android:name=".CardInfoActivity"
            android:windowSoftInputMode="stateHidden|stateAlwaysHidden"/>
        <activity android:name=".PersonalInfoActivity" />
        <activity android:name=".DailyActivity" />
        <activity android:name=".CardActivity" />
        <activity android:name=".DangerActivity" />
        <activity android:name=".OutInActivity" />
        <activity android:name=".LoginActivity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/shape1" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.heweather.plugin.view.SdkWebViewActivity"></activity>
    </application>

</manifest>